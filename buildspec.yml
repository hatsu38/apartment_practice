version: 0.2

phases:
  install:
    runtime-versions:
      ruby: 2.7
    commands:
      - echo "Install Start!"
      - ruby -v

  build:
    commands:
      - echo "Build Start!!!"
      - echo $CODEBUILD_RESOLVED_SOURCE_VERSION